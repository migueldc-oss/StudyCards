# StudyCards 用户手册

**版本 1.1**  
**支持平台：** iOS 18+ • macOS 16+

---

## 目录

1. [欢迎使用](#1-欢迎使用)
2. [快速入门](#2-快速入门)
3. [整理和排序](#3-整理和排序)
4. [学习模式](#4-学习模式)
5. [导入和导出](#5-导入和导出)
6. [分享闪卡](#6-分享闪卡)
7. [设置](#7-设置)
8. [使用技巧](#8-使用技巧)

---

## 1. 欢迎使用

欢迎使用 **StudyCards** —— 一款功能强大且简洁优雅的闪卡应用，适用于 iOS 和 macOS。创建、管理和学习您的闪卡，支持 LaTeX 公式、图片以及无缝的导入/导出功能。

<img src="screenshots/ios/main.png" alt="StudyCards — iOS" width="500">
<img src="screenshots/macos/main.png" alt="StudyCards — macOS" width="500">

**主要功能：**

- **闪卡组：** 按主题或科目组织您的闪卡。
- **完整的闪卡：** 每张卡片都有正面和背面，支持文本、LaTeX 公式和图片。
- **手动排序：** 拖拽闪卡以更改其在组内的顺序。
- **编辑闪卡组：** 通过上下文菜单（长按）编辑或删除闪卡组。
- **排序和搜索：** 按名称、日期或闪卡数量对闪卡组进行排序和搜索。
- **学习模式：** 使用简单/不确定/不会来评估您的掌握程度。
- **导入和导出：** 通过 CSV 或 JSON 文件（包含嵌入的 base64 图片）传输闪卡。
- **分享：** 通过电子邮件、WhatsApp、AirDrop 等发送闪卡。
- **可自定义设置：** 外观、语言和学习顺序。
- **自适应设计：** 自动适配 iPhone、iPad 和 Mac。

### 系统要求

- iOS 18.0 或更高版本 / macOS 16.0 或更高版本。
- 仅在渲染 LaTeX 公式（MathJax）时需要网络连接。

---

## 2. 快速入门

### 2.1 创建闪卡组

闪卡组帮助您按主题、科目或类别组织闪卡。

**操作步骤：**

1. 打开 StudyCards。
2. 在主屏幕上点击 **+** 按钮。
3. 输入闪卡组的名称（例如："西班牙语词汇"、"数学公式"）。
4. 可选择添加描述以标识闪卡组的内容。
5. 点击 **保存** 确认。

<img src="screenshots/ios/create_group.png" alt="创建闪卡组" width="500">

<img src="screenshots/macos/create_group.png" alt="创建闪卡组 - macOS" width="500">

**组织闪卡组的建议：**

- 使用描述性的名称以便于识别。
- 考虑按科目、章节或难度级别创建闪卡组。
- 您可以根据需要创建任意数量的闪卡组。

### 2.2 编辑或删除闪卡组

您可以直接从列表中编辑或删除闪卡组。

**在 iOS 上：**

1. 长按闪卡组名称，直到出现上下文菜单。
2. 选择 **编辑** 以修改名称或描述。
3. 选择 **删除** 以移除该闪卡组及其所有闪卡。

**在 macOS 上：**

1. 右键点击闪卡组名称。
2. 选择 **编辑** 或 **删除**。

> **警告：** 删除闪卡组将永久移除其所有闪卡。如需备份，请先导出。

### 2.3 创建闪卡

进入闪卡组后，您可以开始添加闪卡。

**操作步骤：**

1. 点击进入闪卡组。
2. 点击 **+** 按钮。
3. 输入 **正面** 文本（问题或提示）。
4. 输入 **背面** 文本（答案或解释）。
5. 可选择点击 **选择图片** 添加 **图片**。
6. 可选择使用 `$...$`（行内）或 `$$...$$`（块级）添加 **LaTeX 公式**。
7. 点击 **保存**。

<img src="screenshots/ios/create_card.png" alt="创建闪卡" width="500">

<img src="screenshots/macos/create_card.png" alt="创建闪卡 - macOS" width="500">

### 2.4 添加 LaTeX 公式

StudyCards 通过 MathJax 支持 LaTeX 渲染，用于数学和科学符号。

| 类型 | 语法 | 示例 |
|------|------|------|
| 行内 | `$f(x) = x^2$` | 函数为 $f(x) = x^2$ |
| 块级 | `$$\int_0^1 x^2 \, dx$$` | 公式单独成行 |

**LaTeX 示例：**

- **一元二次方程：** `$x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$`
- **勾股定理：** `$a^2 + b^2 = c^2$`
- **定积分：** `$$\int_a^b f(x)\,dx = F(b) - F(a)$$`

> **注意：** 保存卡片和学习会话期间，公式会自动渲染。

### 2.5 添加图片

您可以为闪卡附加图片以辅助视觉学习。

**操作步骤：**

1. 创建或编辑闪卡时，点击 **选择图片**。
2. 选择拍摄新照片或从相册中选择。
3. 图片将附加到闪卡上。

<img src="screenshots/ios/add_image.png" alt="添加图片" width="500">

<img src="screenshots/macos/add_image.png" alt="添加图片 - macOS" width="500">

> **注意：** 图片会自动调整大小，最大为 1024 像素，以优化存储空间。导出为 JSON 时，图片以 base64 格式包含在内。

### 2.6 删除闪卡

您可以通过向左滑动来删除闪卡。

**操作步骤：**

1. 在闪卡列表中，向左滑动卡片。
2. 将出现一个红色的 **删除** 按钮。
3. 点击 **删除** 确认。

<img src="screenshots/ios/swipe_delete.png" alt="滑动删除" width="500">

<img src="screenshots/macos/swipe_delete.png" alt="滑动删除 - macOS" width="500">

> **警告：** 删除操作是永久的。如无备份，请在删除前先导出闪卡。

---

## 3. 整理和排序

### 3.1 手动排序闪卡

您可以通过拖拽来更改闪卡组内闪卡的顺序。

**在 iOS 上：**

1. 打开包含要排序闪卡的闪卡组。
2. 点击工具栏中的 **编辑**（左上角）。
3. 每张闪卡旁边将出现拖拽手柄 ⟟。
4. 将闪卡拖动到所需位置。
5. 点击 **完成** 保存新顺序。

**在 macOS 上：**

1. 打开包含要排序闪卡的闪卡组。
2. 点击工具栏中的 **排序** 按钮（↕ 图标）。
3. 将闪卡拖动到所需位置。
4. 点击 **完成** 退出排序模式。

> **注意：** 导入和导出闪卡时，手动顺序会被保留。

### 3.2 排序闪卡组

您可以按不同条件对闪卡组列表进行排序。

**操作步骤：**

1. 点击工具栏中的排序按钮（↕ 图标）。
2. 选择以下排序条件之一：

| 条件 | 描述 |
|------|------|
| **名称 A-Z** | 按字母升序排列 |
| **名称 Z-A** | 按字母降序排列 |
| **最新** | 最近创建的闪卡组排在前面 |
| **最旧** | 最早创建的闪卡组排在前面 |
| **闪卡数量** | 闪卡数量最多的闪卡组排在前面 |

> **注意：** 排序同时应用于 **闪卡组** 和 **学习** 部分。

### 3.3 搜索闪卡组

**在 iOS 上：**

1. 在闪卡组列表上下拉以显示搜索栏。
2. 输入要查找的闪卡组名称。

**在 macOS 上：**

1. 使用侧边栏顶部的搜索栏。
2. 输入要查找的闪卡组名称。

---

## 4. 学习模式

学习模式是 StudyCards 的核心功能。它允许您交互式地复习闪卡并评估您的知识水平。

### 4.1 开始学习会话

**操作步骤：**

1. 在导航栏中选择 **学习** 标签。
2. 查找并选择要学习的闪卡组。
3. 第一张闪卡将显示在屏幕上。

<img src="screenshots/ios/start_study.png" alt="开始学习" width="500">

<img src="screenshots/macos/start_study.png" alt="开始学习 - macOS" width="500">

### 4.2 在卡片间导航

在学习会话期间：

1. 阅读卡片的 **正面**（问题）。
2. 思考答案。
3. 点击 **显示答案** 或点击卡片以显示 **背面**（答案）。
4. 使用以下三个按钮之一评估您的掌握程度：

| 按钮 | 含义 | 描述 |
|------|------|------|
| 🟢 **简单** | 您很熟悉 | 卡片将被标记为已掌握，出现频率将降低。 |
| 🟡 **不确定** | 您勉强记住 | 卡片将很快再次出现以加强记忆。 |
| 🔴 **不会** | 您不知道 | 卡片将被重复更多次，直到您学会为止。 |

**键盘快捷键（macOS）：**

| 按键 | 操作 |
|------|------|
| **空格** | 翻转卡片 |
| **1** | 评价为简单 |
| **2** | 评价为不确定 |
| **3** | 评价为不会 |

<img src="screenshots/ios/study_session.png" alt="学习模式" width="500">

<img src="screenshots/macos/study_session.png" alt="学习模式 - macOS" width="500">

### 4.3 完成学习会话

当您复习完闪卡组中的所有卡片后：

1. 将出现完成摘要。
2. 您将看到有多少卡片被评为 *简单*、*不确定* 和 *不会*。
3. 点击 **再次学习** 重复学习，或点击 **返回闪卡组** 退出。

<img src="screenshots/ios/study_summary.png" alt="学习摘要" width="500">

<img src="screenshots/macos/study_summary.png" alt="学习摘要 - macOS" width="500">

---

## 5. 导入和导出

StudyCards 允许您通过 CSV 和 JSON 文件在设备间传输闪卡或创建备份。

### 5.1 导出闪卡

**操作步骤：**

1. 打开要导出的闪卡组。
2. 点击操作菜单并选择 **导出**。
3. 选择导出格式：
   - **CSV：** 电子表格格式。适合在 Excel、Numbers 等中打开。不包含图片。
   - **JSON：** 完整格式。包含文本、LaTeX 公式和 base64 图片。
4. 选择保存文件的位置（文件、iCloud 云盘等）。

<img src="screenshots/ios/export.png" alt="导出" width="500">

<img src="screenshots/macos/export.png" alt="导出 - macOS" width="500">

### 5.2 JSON 文件结构

导出的 JSON 文件具有以下结构：

```json
{
  "groupName": "西班牙语词汇",
  "version": "1.0",
  "createdAt": "2026-07-12T10:30:00Z",
  "cards": [
    {
      "front": "法国的首都是哪里？",
      "back": "巴黎。",
      "frontLatex": null,
      "backLatex": null,
      "difficulty": 0,
      "frontImage": "base64编码字符串...",
      "backImage": null
    }
  ]
}
```

### 5.3 CSV 文件结构

CSV 文件使用以下列：

```csv
front,back,frontLatex,backLatex,difficulty,frontImage,backImage
"法国的首都是哪里？","巴黎。","","","","",""
```

> **注意：** CSV 格式中的图片以 base64 字符串形式导出到 `frontImage` 和 `backImage` 列中。

### 5.4 导入闪卡

**操作步骤：**

1. 在闪卡组的闪卡列表中，点击操作菜单并选择 **导入**。
2. 从设备中选择 CSV 或 JSON 文件。
3. StudyCards 将把闪卡导入到当前闪卡组中。

<img src="screenshots/ios/import.png" alt="导入" width="500">

<img src="screenshots/macos/import.png" alt="导入 - macOS" width="500">

> **注意：** 如果导入包含 base64 图片的 JSON 文件，图片将正确显示在闪卡中。

### 5.5 常见导入错误

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| *格式无法识别* | 文件不是有效的 CSV 或 JSON | 验证文件具有正确的扩展名且未损坏 |
| *空文件* | 文件不包含任何卡片 | 检查文件是否至少包含一行数据 |

---

## 6. 分享闪卡

StudyCards 使您能够轻松地与他人分享闪卡。

### 6.1 将闪卡组分享为 JSON

**操作步骤：**

1. 打开要分享的闪卡组。
2. 点击操作菜单并选择 **分享**。
3. 系统分享面板将打开。
4. 选择要发送的应用或方式：
   - **AirDrop** —— 用于附近的 Apple 设备。
   - **邮件** —— 将 JSON 文件作为附件发送。
   - **WhatsApp** —— 将 JSON 文件作为文档发送。
   - **信息** —— 通过 iMessage 发送。
   - **其他应用** —— 任何接受文件附件的应用。

<img src="screenshots/ios/share.png" alt="分享" width="500">

<img src="screenshots/macos/share.png" alt="分享 - macOS" width="500">

### 6.2 接收共享的闪卡

当有人向您发送 StudyCards JSON 文件时：

1. 在设备上打开该文件。
2. 选择 **在 StudyCards 中打开** 或 **导入**。
3. 闪卡组将自动添加到您的收藏中。

---

## 7. 设置

自定义 StudyCards 以满足您的偏好。

### 7.1 外观

控制应用的视觉主题。

| 选项 | 描述 |
|------|------|
| **系统** | 自动匹配设备的外观设置（浅色或深色）。 |
| **浅色** | 始终使用浅色模式。 |
| **深色** | 始终使用深色模式。 |

**操作步骤：**

1. 进入 **设置** 标签。
2. 在 **外观** 部分，选择三个选项之一。
3. 更改将立即生效。

<img src="screenshots/ios/appearance.png" alt="外观设置" width="500">

<img src="screenshots/macos/appearance.png" alt="外观设置 - macOS" width="500">

### 7.2 语言

StudyCards 提供 8 种语言。您可以强制使用特定语言或使用系统默认语言。

| 语言 | 代码 |
|------|------|
| **系统** | 自动选择设备语言 |
| Español | es |
| English | en |
| Português | pt |
| Français | fr |
| Deutsch | de |
| Italiano | it |
| 日本語 | ja |
| 中文 | zh |

**操作步骤：**

1. 进入 **设置** 标签。
2. 在 **语言** 部分，选择所需的语言。
3. 更改将立即生效，无需重启。

<img src="screenshots/ios/language.png" alt="语言设置" width="500">

<img src="screenshots/macos/language.png" alt="语言设置 - macOS" width="500">

### 7.3 学习顺序

设置学习模式中闪卡的默认显示顺序。

| 选项 | 描述 |
|------|------|
| **顺序** | 按创建顺序显示卡片（如果已手动排序则按手动顺序）。 |
| **随机** | 每次学习会话中随机打乱卡片顺序。 |

**操作步骤：**

1. 进入 **设置** 标签。
2. 在 **学习顺序** 部分，选择您的偏好。
3. 更改将应用于所有未来的学习会话。

<img src="screenshots/ios/study_order.png" alt="学习顺序设置" width="500">

<img src="screenshots/macos/study_order.png" alt="学习顺序设置 - macOS" width="500">

---

## 8. 使用技巧

### 整理技巧

- **为每个科目或主题创建闪卡组。** 这将帮助您快速找到需要学习的内容。
- **使用简短清晰的名称** 为闪卡组和闪卡命名。避免在屏幕上难以阅读的过长文本。
- **手动排序闪卡** 将最重要的放在前面。
- **定期复习您的闪卡组。** 删除已掌握的卡片，并添加新卡片以匹配您的进度。

### 学习技巧

- **使用间隔重复。** 在不同日期学习相同的卡片以巩固长期记忆。
- **不要忽略困难的卡片。** 如果您将卡片评为 *不会*，请尽快再次学习该闪卡组。
- **混合学习模式。** 在顺序和随机模式之间交替，避免记住卡片的位置。
- **使用键盘快捷键** 在 macOS 上更快地学习（空格、1、2、3）。
- **进行短时间学习。** 10 到 20 分钟足以进行一次高效的学习。

### 内容技巧

- **编写清晰简洁的问题。** 避免歧义。
- **每张卡片一个核心概念。** 不要在正面或背面堆砌过多信息。
- **使用图片。** 图表或图片可能比一段文字更有效。
- **使用 LaTeX 公式** 进行数学或科学内容。它们精确且看起来专业。

### 备份技巧

- **定期导出闪卡组。** 将副本保存到 iCloud 云盘或电脑。
- **使用 JSON 格式** 以保留图片。CSV 仅保存文本。
- **与同学或同事分享闪卡** 以协作学习。

### 性能技巧

- **学习时关闭其他应用** 以确保 StudyCards 无中断运行。
- **保持应用更新** 以获取最新功能和错误修复。
- **如果 LaTeX 公式未渲染**，请检查网络连接。MathJax 首次需要下载资源。

---

## 法律信息

- **StudyCards** 是一款独立开发的应用。
- MathJax 的使用受其 Apache 2.0 许可证约束。
- 您的数据仅存储在设备上。StudyCards 不会收集或共享个人信息。

---

*StudyCards 用户手册 — 版本 1.1 — 2026年7月*
